import yaml


def yaml_to_env(config_file: str) -> str:
    env_array = ""

    def recursive_parser(config_file: str):
        return None

    if len(config_file, env_array) > 0:


    for idx, el in  enumerate(config_file.split(sep="\n")):
        if el.endswith(":"):
            yaml_to_env(config_file[idx:]: str) -> str:
        else:
            return
def env_to_yaml(env_list: str) -> str:
    ...